/**
 * Configuration classes automatically detected by Spring during application startup, providing
 * implementations and instances incorporated into the security filter chain and injected into other
 * application classes.
 */
package edu.cnm.deepdive.picmegallery.configuration;